# ===========================================
# Medical Exams Parser Configuration
# ===========================================

# Primary vision model for extraction
EXTRACT_MODEL_ID=google/gemini-3-flash-preview

# Model for text summarization
SUMMARIZE_MODEL_ID=google/gemini-3-flash-preview

# Model for self-consistency voting
SELF_CONSISTENCY_MODEL_ID=google/gemini-3-flash-preview

# Input/Output paths
INPUT_PATH=/path/to/exam/pdfs
INPUT_FILE_REGEX=.*\.pdf
OUTPUT_PATH=/path/to/output

# OpenRouter API key
OPENROUTER_API_KEY=your_api_key

# OpenRouter base URL (default: https://openrouter.ai/api/v1)
# Override for local LLM proxy like claude-code-bridge
# OPENROUTER_BASE_URL=http://127.0.0.1:8000/v1

# Number of extraction attempts for self-consistency
N_EXTRACTIONS=3

# Maximum parallel page extractions per document (default: 1)
# Increase to speed up processing (recommended: 5-10 for fast APIs)
# Note: Each worker makes API calls, so higher values increase concurrent requests
MAX_WORKERS=5
